name: transactions-pysr-validation-legacy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired workflow notice
        run: |
          echo "This legacy workflow rejected incomplete PySR equations instead of retaining them as failures."
          echo "Use transactions_pysr_validation_v2.yml for the frozen validation study."
