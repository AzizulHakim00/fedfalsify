# Phase-3C Fresh-Development Protocol — DRAFT, NOT AUTHORIZED

**Date:** 2026-09-12  
**Base sealed branch:** `research/phase3b-scientific-implementation`  
**Sealed Phase-3B head:** `309fd9419ac321322cb99289e2d8f168eded3a98`  
**Frozen scientific implementation source:** `b67a07371bcf244728536028593373ca7d1990b1`  
**Fresh development seeds:** `29301–29310`  
**Final-confirmation seeds:** `11001–11999` remain blocked  
**Status:** prospective draft only; this document does not authorize execution.

## 1. Scientific objective

Evaluate whether FedFalsify v12 / SCSV-NCSC improves structural recovery over frozen v11 under the complete V10 heterogeneous federated symbolic benchmark while preserving false-positive control, shared-core quality, localized-mechanism quality, communication cost, runtime, and implementation integrity.

The development block is a single governed experiment. Once any seed in `29301–29310` is executed scientifically, the entire block is considered spent. No algorithm, threshold, grammar, role rule, split rule, multiplicity rule, capacity, rank policy, or evaluation definition may change within the block.

## 2. Scientific methods

Exactly four governed methods are evaluated per condition, in fixed order:

1. `scsv-elrc-v11-full` — frozen v11 comparator;
2. `scr-only` — SCR shared core + frozen v11 localized path;
3. `ncee-only` — frozen v11 shared core + NCEE localized path;
4. `scsv-ncsc` — SCR shared core + NCEE localized path.

No external baseline is part of the Phase-3C GO/NO-GO gate. PySR, Operon, FSL-GEP, NRSR, multitask SR, independent symbolic families, and real-world data belong to a later external-validation suite after the internal development decision is frozen.

## 3. Dataset and condition matrix

The primary dataset is the frozen V10 synthetic federated symbolic benchmark generated by `scsv_v10_benchmarks.py`.

Per seed, the legal condition matrix contains exactly 120 conditions:

- `quadratic_role_v10`, `linear_role_v10`, `trig_role_v10`, `interaction_role_v10`: `K∈{4,8,16}`, balanced/imbalanced, single role for all K, quarter role for K∈{8,16}, noise `0.10/0.30` → 20 conditions per family, 80 total;
- `null_role_v10`, `anchor_contamination_null_v10`: `K∈{4,8,16}`, balanced/imbalanced, role=`none`, noise `0.10/0.30` → 12 conditions per family, 24 total;
- `weak_source_role_v10`, `dual_role_v10`: `K∈{8,16}`, balanced/imbalanced, role=`quarter`, noise `0.10/0.30` → 8 conditions per family, 16 total.

Across ten fresh seeds: `120 × 10 = 1200` matched conditions. With four methods: `4800` primary method rows.

Nominal samples/client=`100`; client counts=`4,8,16`; balance=`balanced/imbalanced`; noise ratio=`0.10/0.30`.

## 4. Data separation and leakage firewall

The frozen v12 implementation keeps the existing deterministic client-local split:

- Discovery: approximately 70% of each client;
- Selector: approximately 15%;
- Probe: approximately 15%.

Discovery may create hypotheses. Selector may only screen/remove. Probe may only certify/reject the frozen surviving family. Benchmark truth is evaluation metadata only and must never enter a scientific acceptance function.

## 5. Frozen scientific constants

The Phase-3B scientific constants remain unchanged:

- shared Holm alpha=`0.05`;
- Discovery role BH q=`0.10`;
- final localized Holm alpha=`0.05`;
- minimum localized active support=`10` rows/client;
- minimum localized residual df=`5`;
- maximum localized role fraction=`0.50`;
- outside-role SSE tolerance=`1e-10`;
- maximum shared non-intercept terms=`5`;
- maximum accepted localized terms=`2`;
- maximum final terms including intercept=`10`;
- deterministic machine-scale rank policy;
- final coefficient refit cannot change accepted structure.

## 6. Primary and secondary outputs

Every method-condition row records:

- exact term-structure recovery;
- term precision/recall;
- shared precision/recall and TP/FP/FN;
- localized-deviation precision/recall and TP/FP/FN;
- exact role/scope agreement for accepted localized mechanisms when identifiable from the governed output ledger;
- mechanism-exact recovery = exact term structure AND exact localized scope;
- test NMSE on the independent 4000-point noiseless global test set;
- runtime and communication bytes;
- shared/localized structures;
- accepted deviations;
- diagnostics ledgers;
- integrity violations.

The historical exact-recovery metric is retained unchanged for comparability. Scope/mechanism-exact metrics are additive diagnostics and do not silently redefine earlier v11 results.

## 7. Paired statistical analysis

The full `scsv-ncsc` method is compared condition-wise with frozen v11.

Report:

- overall exact-recovery gain;
- repairs: v11 wrong → full correct;
- harms: v11 correct → full wrong;
- exact McNemar/binomial test on discordant pairs;
- seed-cluster bootstrap 95% CI for exact-recovery gain, using a fixed analysis RNG distinct from benchmark seeds;
- exact one-sided sign-flip/randomization p-value over the ten seed-level gain estimates;
- seed-level gain table.

Analysis RNG is not a benchmark-generation seed and does not consume the scientific seed namespace.

## 8. Prospective development gate

The development decision uses only the prespecified criteria below. Engineering seed `29300` is not used to alter them.

- overall exact-recovery gain vs v11 `>= +0.03`;
- seed-cluster bootstrap lower 95% CI bound for gain `> 0`;
- pooled deviation precision `>= 0.99`;
- pooled deviation recall `>= 0.94`;
- pooled shared precision `>= 0.975`;
- pooled shared recall `>= 0.98`;
- null localized-FP condition rate `<= 0.02`;
- null shared-FP condition rate `<= 0.02`;
- exact harm rate vs v11 `<= 0.01` of all matched conditions;
- main-family all-true-deviation recovery for each of the four ordinary role families `>= 0.92`;
- all-true-deviation recovery by client count: K=4 `>=0.90`, K=8 `>=0.93`, K=16 `>=0.95` on applicable non-null conditions;
- high-noise main-family all-true-deviation recovery `>=0.90`;
- high-noise main-family exact-recovery gain vs v11 `>=+0.05`;
- weak-source all-true-deviation recovery `>=0.90`;
- dual-role all-true-deviation recovery `>=0.90`;
- median communication ratio full/v11 `<=1.25`;
- median runtime ratio full/v11 `<=1.50`;
- zero row-level integrity violations;
- exact 1200-condition / 4800-row completeness and exact four-method grouping.

All criteria are conjunctive for `PHASE3-DEVELOPMENT-GO`. Otherwise the decision is `PHASE3-DEVELOPMENT-NO-GO`. McNemar and sign-flip p-values are reported as paired inferential diagnostics but are not additional hidden gates.

## 9. Checkpoint and persistence rules

Checkpoint only complete four-method condition groups. A partial, duplicate, unauthorized, wrong-seed, or wrong-method group is discarded on resume. Every completed group is atomically persisted before the next condition begins.

The Google Drive target is:

`/content/drive/MyDrive/FedFalsify_Q1/SCSV_NCSC_PHASE3_DEVELOPMENT`

Artifacts include transparent CSV/JSON/TXT archival truth plus PKL/joblib convenience state and a SHA256 manifest/ZIP. Performance summaries are written only after all 1200 conditions are complete; live execution may display progress and integrity status but must not display comparative performance that could invite mid-block tuning.

## 10. Authorization firewall

The runner must require all of the following before a scientific row is generated:

- exact ten-seed block `29301–29310`;
- test gate environment variable=`PASS`;
- explicit runtime authorization token=`AUTHORIZE_PHASE3C_FRESH_DEVELOPMENT`;
- frozen protocol SHA matches the runner-declared SHA;
- scientific files are byte/diff-equivalent to the frozen Phase-3B scientific source;
- final-confirmation seeds `11001–11999` remain unreachable.

The protocol is frozen only after tests and CI are green and its final SHA is recorded. Until then, execution must fail closed.
